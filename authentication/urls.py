from django.urls import path
from . import views

urlpatterns = [
    #path("", views.LoginAPIView.as_view(), name=""),
    # path("register", views.RegistrationAPIView.as_view(), name="register"),
]
